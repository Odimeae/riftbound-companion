/**
 * WlPill — design-system name for the soft W/L outcome pill.
 * Implementation lives in OutcomeBadge; this re-exports for Matches/Sideboard P0.
 */
export { OutcomeBadge as WlPill, OutcomeBadge } from './OutcomeBadge';
